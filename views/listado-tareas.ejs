<div class="row align-items-center mb-4">
  <div class="col">
    <h2 class="text-primary">Mis Tareas</h2>
    <p class="text-muted">Gestiona tus pendientes del día</p>
  </div>
  <div class="col-auto">
    <a href="/tareas/crear" class="btn btn-success shadow-sm">
      + Nueva Tarea
    </a>
  </div>
</div>

<div class="card shadow-sm">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table table-hover table-striped mb-0">
        <thead class="table-dark">
          <tr>
            <th scope="col" style="width: 5%;">#</th>
            <th scope="col">Descripción</th>
            <th scope="col" style="width: 15%;" class="text-end">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% if (tareas.length > 0) { %>
            <% tareas.forEach(function(tarea) { %>
              <tr>
                <td class="align-middle"><%= tarea.id %></td>
                <td class="align-middle fw-medium"><%= tarea.descripcion %></td>
                <td class="text-end">
                  <form action="/tareas/eliminar" method="POST" style="display: inline;">
                    <input type="hidden" name="id" value="<%= tarea.id %>">
                    <button type="submit" class="btn btn-sm btn-outline-danger">
                      Eliminar
                    </button>
                  </form>
                </td>
              </tr>
            <% }); %>
          <% } else { %>
            <tr>
              <td colspan="3" class="text-center py-5 text-muted">
                <h4>🎉 ¡Todo limpio!</h4>
                <p>No tienes tareas pendientes por ahora.</p>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
</div>